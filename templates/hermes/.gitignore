# templates/hermes — local artifacts.

# Persistent agent state: auth tokens, provider config, sessions, logs.
# Never commit.
state/*
!state/.gitkeep

# Default workspace dir (used when WORKSPACE points here).
workspace/*
!workspace/.gitkeep

# Local environment with potentially-sensitive values.
.env
